{
  "name": "github-detial-comment",
  "displayName": "Github detail comment",
  "description": "Create github comment from Visual Studio Code",
   "repository": {
      "type": "git",
      "url": "https://github.com/wk-j/github-detail-comment"
   },
  "version": "0.0.1",
  "publisher": "wk-j",
  "engines": {
    "vscode": "^1.5.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onCommand:extension.githubDetailComment"
  ],
  "main": "./out/src/extension",
  "contributes": {
    "commands": [
      {
        "command": "extension.githubDetailComment",
        "title": "Create Gitub Comment"
      }
    ]
  },
  "scripts": {
    "vscode:prepublish": "tsc -p ./",
    "compile": "tsc -watch -p ./",
    "postinstall": "node ./node_modules/vscode/bin/install"
  },
  "devDependencies": {
    "typescript": "^2.0.3",
    "vscode": "^1.0.0",
    "mocha": "^2.3.3",
    "@types/node": "^6.0.40",
    "@types/mocha": "^2.2.32"
  },
  "dependencies": {
    "octonode": "^0.7.6"
  }
}
